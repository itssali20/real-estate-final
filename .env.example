# ── Contact routing (all optional — the form works without them) ────────────
# Generic webhook: HubSpot, Salesforce, Zapier, Make, n8n, Slack, anything.
# Receives { receivedAt, pipeline, type, source, data } as JSON.
CRM_WEBHOOK_URL=

# Transactional email via Resend (https://resend.com)
RESEND_API_KEY=
CONTACT_TO_EMAIL=investors@concordpacificcorp.com
CONTACT_FROM_EMAIL=website@concordpacificcorp.com
